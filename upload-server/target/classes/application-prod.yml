# 生产环境配置
spring:
  datasource:
    url: jdbc:mysql://192.168.0.79:3306/test_02?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=Asia/Shanghai
    username: mls01
    password: 12345@Mls
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5

# 生产环境特征提取服务配置
app:
  feature:
    extraction:
      docker:
        enabled: true  # 生产环境启用
        url: http://192.168.0.79:5000/extract

# 生产环境日志配置
logging:
  level:
    root: INFO
    com.mls.upload.server: INFO
    org.springframework.web: WARN
    org.mybatis: WARN
  file:
    name: /var/log/upload-server/application.log
